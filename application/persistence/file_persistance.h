#pragma once
#include <persistence_base.h>

namespace core
{
    template <typename T>
    class file_persistance : public persistance_base
    {
        void persist_impl(const char* data)
        {
           
        }
    };
}
